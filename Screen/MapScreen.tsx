import React from 'react';
import {
  View,
  Text,
} from 'react-native';
import { MapComponent } from '../Component';

const MapScreen = () => {
	return (
		<View>
			<MapComponent />
		</View>
	);
}

export default MapScreen;