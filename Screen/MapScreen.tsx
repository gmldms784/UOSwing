import React from 'react';
import {
  View,
} from 'react-native';
import { MapComponent } from '../Component';

// 맵 페이지
const MapScreen = () => {
	return (
		<View>
			<MapComponent/>
		</View>
	);
}

export default MapScreen;