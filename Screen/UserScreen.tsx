import React from 'react';
import {
  View,
  Text
} from 'react-native';
import { MapComponent } from '../Component';

const UserScreen = () => {
	return (
		<View>
			<MapComponent />
		</View>
	);
}

export default UserScreen;