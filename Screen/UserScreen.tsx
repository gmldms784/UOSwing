import React from 'react';
import {
  View,
  Text
} from 'react-native';
import { MapComponent } from '../Component';

// 사용자 페이지
const UserScreen = () => {
	return (
		<View>
			<MapComponent />
		</View>
	);
}

export default UserScreen;